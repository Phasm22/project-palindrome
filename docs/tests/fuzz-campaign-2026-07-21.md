# Fuzz Campaign — 2026-07-21

**Branch:** `realAgent` @ `bcfe65d` (HEAD at campaign start)
**Scope:** End-to-end fuzz of the live Palindrome agent (`POST /api/agent/query` + SSE `/api/agent/stream`), grounded in real twin data.
**Owner:** autonomous campaign run in a single session, per the task brief.

## Artifacts

| File | Purpose |
|---|---|
| `docs/tests/ground-truth-snapshot-2026-07-21.json` | Twin snapshot (nodes/VMs/interfaces/subnets/switch/firewall rules+aliases) pulled directly from Neo4j, used to grade every response |
| `docs/tests/fuzz-corpus-2026-07-21.json` | 128 single-turn + 8 multi-turn (18 total turns) queries across 8 categories = **146 agent invocations** |
| `docs/tests/fuzz-results-2026-07-21.jsonl` | Raw captured results (request, full response JSON incl. `structuredResponse`/`rawTextFallback`, reasoning trace where available) — one JSON object per line |
| `docs/tests/fuzz-reverify-corpus-2026-07-21.json` / `fuzz-reverify-results-2026-07-21.jsonl` | Phase 7 targeted live re-verification of every fix, run twice against freshly restarted instances |
| `scripts/fuzz-campaign-runner.ts` | Reusable harness: drives the corpus against the live API via SSE, appends JSONL incrementally. `FUZZ_CORPUS_PATH=<file> bun run scripts/fuzz-campaign-runner.ts <outPath> <concurrency> [idPrefixFilter]` |
| `scripts/fuzz-campaign-summarize.ts` | Renders a JSONL results file into a compact human-readable summary |

## Methodology

- Ground truth pulled by connecting directly to Neo4j (`TwinEntity` nodes) rather than through the agent, so grading is independent of the thing under test.
- Corpus queries reference **real, live entity names** (nodes `YANG`/`yin`/`proxBig`; VMs `windowsVM`, `pihole`, `sentinelZero`, `sentinel-hunter`, etc.; switch `TJswitch`; firewall aliases `WG_VIP`, `Home_DNS`, `bogons`, ...) plus deliberately-wrong-cased/typo'd/nonexistent variants for edge cases.
- Driven via `POST /api/agent/query` (fire-and-forget, returns `sessionId` immediately) + `GET /api/agent/stream?sessionId=...` (SSE) to capture the `agent:final` event, which carries `text`, `structuredResponse`, `rawTextFallback`, `conversationState`, and `traceId`. **Note:** `docs/API_REFERENCE.md`'s description of `/api/agent/query` as synchronous (`{success, response, toolCalls}`) is stale — the real contract is async-start + SSE-final, matching what `dashboard/js/chat.js` actually does. Worth a docs fix separately (not done here, out of scope).
- Concurrency 5, with a distinct `userId` per concurrent worker (the server's `activeRunByUser` guard serializes runs per-user, so same-`userId` concurrent requests 409).
- For every non-PASS finding, pulled the full reasoning trace via `GET /api/dashboard/reasoning-traces/{traceId}` and, where the crash pre-dated any trace being recorded, cross-referenced the live server log (`logs/palindrome-api.log`) by session ID/timestamp.

## Environment

- `pc-stacks up palindrome` (with `PCE_INGESTION_ENABLED=1`) — stack was already warm-ish at session start (one stale `pce-api` process from an earlier attempt that day); restarted cleanly twice during this campaign (once to load Phase-6 fixes, once to load a fix discovered after the first restart). Neo4j data survives container recreation (named volume) — confirmed via direct Cypher checks each time.
- Ingestion scheduler confirmed healthy throughout (`proxmox`/`network`/`switch`/`topology` all `success: true` on every 5-min cycle; `firewall` reports `entities: 0` — this is `createRuleRelationships` not yet handling NAT/rdr actions, a known Phase-2 gap per the vision-gap memory, not a bug).

---

## Phase 4 — Findings by category

Legend: **PASS** correct+well-formed · **GRACEFUL GAP** known roadmap limitation, degrades cleanly · **ROUTING BUG** wrong tool/chain/regex-bypass selected · **WRONG ANSWER** contradicts ground truth · **FORMAT BREAK** data right, shape wrong · **CRASH/MAXSTEPS** unhandled exception or step-budget exhaustion.

| Category | Units | PASS | GRACEFUL GAP | ROUTING BUG | WRONG ANSWER | FORMAT BREAK | CRASH | MAXSTEPS | Other |
|---|---|---|---|---|---|---|---|---|---|
| A. Tool-surface (twin/proxmox/opnsense/ssh/diag/action) | 61 | 42 | 0 | 5 | 6 | 3 | 3 | 2 | — |
| B. Known-failure regression | 10 | 8 | 0 | 0 | 2 | 0 | 0 | 0 | — |
| C. Vision-gap probes | 10 | 3 | 4 | 2 | 1 (fixed) | 0 | 0 | 0 | — |
| D. Entity edge cases | 15 | 15 | 0 | 0 | 0 | 0 | 0 | 0 | — |
| E. Format stress | 10 | 6 | 1 | 1 | 0 | 1 | 1 | 0 | — |
| F. Composite/multi-domain | 10 | 4 | 0 | 0 | 3 (1 fixed) | 1 | 1 | 1 | — |
| H. Adversarial | 12 | 8 | 3 | 0 | 1 | 0 | 0 | 0 | — |
| G. Multi-turn (8 flows) | 8 | 4 | 0 | 0 | 1 | 0 | 0 | 0 | 3 partial-continuity |
| **Total (136 units / 146 invocations)** | | **90** | **8** | **8** | **14** | **5** | **5** | **3** | **3** |

**Headline:** the single largest, highest-leverage bug class was **CRASH** — every crash in the campaign (5 in the original run, plus 2 more surfaced while re-verifying other fixes = 7 distinct instances) traced to exactly **two root causes**, both fixed (§ Phase 6). After the fix, **0/16** targeted live re-verification queries crash; the worst remaining outcome for a previously-crashing query is a clean `MAXSTEPS` message.

---

## Phase 5 — Root-cause map

### Fixed this session (Phase 6)

#### 1. CRASH — `executeToolCall` doesn't catch tools that `throw` instead of returning `{error}`
**File:** `src/agent/tool-executor.ts`
**Evidence:** `A-TQ-11`, `A-PX-06` (original run), reproduced live 2x more in isolation (`repro-test-1`, `RV-CRASH-01`).
**Root cause:** `tool.execute()` was awaited with no `try/catch`. Several read-only tool actions (e.g. `proxmox_readonly`'s `node_disks`/`node_network_interfaces`) validate required params via `throw new Error(...)` rather than returning `{ error }`. An uncaught rejection propagates through `handleExecute` → `runAgent` → the top-level `.catch()` in `server.ts`, which emits `"The agent run failed before it completed."` — bypassing tool-error handling, failure-reclassification, and graceful degradation entirely.
**Fix:** wrap the `tool.execute()` call in try/catch, converting a thrown error into a normal `{ error }` `ExecutionResult` (matching the contract every caller already expects).
**Test:** `tests/agent/tool-executor.test.ts` (async throw, sync throw, healthy-tool passthrough, unknown-tool passthrough).

#### 2. CRASH — mid-batch `context.addUserMessage()` breaks OpenAI's tool_calls ordering contract
**File:** `src/agent/handlers/handle-execute.ts`
**Evidence:** `A-TQ-11`, `A-OP-06`, `E-03`, `F-05` (original run); `RV-ASKMISSING-01`, `RV-CRASH-01`, `RV-CRASH-05` (surfaced again while re-verifying other fixes, with fresh session IDs at a different time — confirmed **reliably reproducible**, not transient).
**Root cause — root-caused via `logs/palindrome-api.log` line-by-line trace, not guesswork:** when the LLM emits 2+ `tool_calls` in one turn and the *first* one fails, the failure-reclassification block calls `context.addUserMessage(...)` **immediately, inside the per-tool-call loop** — before the *remaining* tool_calls in the same batch have been processed. Since the assistant message carrying *all* `tool_calls` for the turn is pushed to context once, up front, this inserts a `user` message between two `tool` response messages that both belong to the same assistant turn. OpenAI's Chat Completions API rejects the *next* call with exactly the observed 400: `"An assistant message with 'tool_calls' must be followed by tool messages responding to each 'tool_call_id'."` This is the failure-reclassification loop-risk that REVIEW.md §4 flagged in the abstract (*"reclassifyIntentWithContext() is called, which modifies the agent context (adds messages)"*) — this campaign found the concrete, reproducible manifestation.
**Fix:** collect the reclassification follow-up message(s) in a local array during the loop; flush them via `context.addUserMessage()` only *after* every `tool_call_id` in the batch has received its tool-role response.
**Test:** not unit-tested — `handleExecute` has no existing test seam (30+ required input fields, module-level singleton OpenAI client, no dependency injection for `tools`/RAG/twin) and the one existing test file that exercises this function (`tests/agent/runner-confirmation-flow.test.ts`) already does so via live `runAgent()` + live OpenAI calls (consistent with this file's existing convention, but not one to add to per the "no live infra in unit tests" rule). **Verified instead via live re-verification** (Phase 7): reproduced the crash 100% (3/3 attempts across 2 separate live sessions before the fix), then confirmed 0/16 crashes after. This is flagged explicitly, not glossed over — see "What I did not unit-test" below.

#### 3. ROUTING BUG — `isClearInformationalQuery()` bypass has gaps, so an LLM classification "bad day" falls into a broken clarification loop
**File:** `src/reasoning/intent-router.ts`
**Evidence:** `A-TQ-01` ("Give me a summary of the whole cluster."), `A-SSH-03/04/06` (ping/traceroute/health-diagnostic imperatives), `A-DIAG-01` ("why can't I reach pihole"), `E-10` ("vms???? on yang???? pls?????").
**Root cause:** `classifyAndRouteWithLLM()` already has a deliberate regex-based safety net (`isClearInformationalQuery()`) for exactly this failure mode (comment: *"Bypass clarification for clear informational questions so we don't ask 'observe, diagnose, change, or explain?'"*), but its pattern list only covers `show/tell/list/describe/what/which/is/are` phrasings. It doesn't cover `"give me"` (extremely common), imperative diagnostic verbs (`ping X`, `traceroute to X`, `diagnose X`), or loose punctuation-heavy phrasing. When the LLM classifier (non-deterministic, `gpt-4o-mini`) has a low-confidence day for one of these gapped phrasings, there's no net, and the user gets the generic "Are you asking to observe, diagnose, change, explain, or plan?" instead of an answer.
**Fix:** extended the pattern list with `give|gimme`, `^(ping|traceroute)`, `check/run + diagnostic/health/...`, `diagnose`, `summar(y|ize)`, and a loose `<entity noun> ... on <target>` catch-all.
**Test:** `tests/reasoning/classify-and-route-conversation.test.ts` (all 6 previously-broken phrasings now bypass clarification; confirmed the test fails without the fix).
**Caveat (see Phase 7):** this is a *backstop*, not a guarantee — it only fires when `routing.route === "clarification"` is reached in the first place. `C-08` ("Would deleting the bogons alias break anything?") and a couple of others in category C still occasionally hit a *different* generic clarification wording (`"What do you want to do next..."`, from the `missing: ["intent"]` path in `handle-clarify.ts`/`AskMissingTool.ts`) that my regex list doesn't cover. Not fixed — flagged as a residual, lower-priority follow-up since it's LLM-day-dependent, not 100%-reproducible.

#### 4. WRONG ANSWER — `extractAliasName()` greedily swallows the rest of the sentence
**File:** `src/reasoning/detectFirewallIntent.ts`
**Evidence:** `F-10` — *"What VMs would be affected if the WG_VIP alias were removed, and are any of them internet-exposed?"* → agent replied *"No alias named `were removed, and are any of them internet-exposed` was found..."*
**Root cause:** the `alias\s+(.+?)(?:contents?|members?|entries?|[.?!]|$)` regex has no bound on ordinary continuation words (`were`, `and`, `removed`, ...), so once "alias" isn't immediately followed by a terminator, it captures until the next period/question-mark — i.e. the rest of the sentence. There was also **no support at all** for the (more natural English) "**the X alias**" word order — only "alias X" and quoted variants.
**Fix:** (a) added a preceding-token pattern (`"the WG_VIP alias"` → `WG_VIP`), tried first; (b) added a stopword-based truncation for the existing forward/backward patterns so a greedy capture stops at the first non-name word; (c) added a guard excluding impact/removal-flavored questions (`break|removed|deleted|affect|impact`) from the "show contents" `alias_contents` branch entirely, so `F-10`-style compound impact questions fall through to the EXECUTE/LLM path (which can actually reason about the compound "impact + exposure" question) instead of the narrow contents-lookup chain. Verified this guard doesn't regress the pre-existing, working `A-TQ-20`/control case (*"What would break if I deleted the Home_DNS alias?"*).
**Test:** `tests/reasoning/detect-firewall-intent.test.ts` (3 new cases; confirmed 2/3 fail without the fix).

#### 5. FORMAT BREAK — `normalizeCountPackaging()` discards real data when it can't parse a bare count
**File:** `src/agent/response-formatter.ts`
**Evidence:** `E-04` — *"List all firewall aliases and how many entries each one has."* → chain correctly computed `"Firewall Rule Count\n- total | Count=104 | PASS=71 | BLOCK=24 | NAT=9"`, but the response the user actually saw was the bare title **`"Firewall Rule Count"`** — every number silently discarded.
**Root cause:** `isCountQuery()` is a blanket `/\bhow\s+many\b/` test with no domain restriction, so it fires on this multi-attribute breakdown question. `normalizeCountPackaging()` then tries (and fails) to extract a single VM/node/container-shaped count from the text, and its fallback was `return lines[0] ?? trimmed;` — i.e. "if in doubt, keep only the title line."
**Fix:** changed the fallback to `return null` (don't touch the response; let normal formatting/passthrough handle it) instead of destructively truncating. Also widened the unit vocabulary (`rules?|aliases?`) for the cases that *do* have a real single count to extract.
**Test:** `tests/agent/response-formatter-adaptive.test.ts` (2 new cases: one confirming the destructive-truncation regression is gone, one confirming genuine single-count extraction still works; confirmed the first fails without the fix).
**Residual, not fixed:** the underlying chain-selection is still semantically wrong for this exact phrasing — `detectFirewallIntent`'s `isCountQuery` routes *any* "how many" phrasing to `count_rules` (rule counting) regardless of whether the user asked about rules, aliases, VMs, or anything else, so `E-04` still gets a rule-count answer instead of a per-alias breakdown. That's a distinct, lower-priority routing gap (needs "count of *what*?" disambiguation, more corpus evidence before a confident fix) — flagged, not fixed.

#### 6. WRONG ANSWER (UX) — `ask_missing`'s LLM fallback echoes raw parameter names to the user
**File:** `src/tools/AskMissingTool.ts`
**Evidence:** `C-10` — *"Which assets have an undocumented path between the home and lab trust zones?"* → *"What is the fromId for the asset in question?"* (leaked the internal `fromId` field name verbatim).
**Root cause:** the tool has deterministic fast-paths for common missing slots (`intent`, `target/node/host`, `vmid`, `name`, `type`) but anything else falls through to a generic LLM call whose system prompt doesn't instruct it to avoid parroting the raw slot name back — and for a slot like `fromId` the small model just... didn't.
**Fix:** added fast-path mappings for `fromId/sourceId`, `chain/interface`, `subnet/cidr`, `ruleId/rule`, `alias/aliasName`, plus an explicit "never mention internal parameter names verbatim" instruction on the remaining LLM-fallback system prompt for anything still unmapped.
**Test:** `tests/tools/ask-missing-tool.test.ts` (6 cases covering all 4 new mappings plus 2 existing behaviors, unaffected).

### Found, prioritized, **not fixed** (flagged for follow-up)

| Finding | Where | Why not fixed here |
|---|---|---|
| `A-TQ-10`/`A-TQ-16`/`A-TQ-19`: `network_vms_by_subnet`, `firewall_rules_blocking_subnet`, `firewall_reachability_from_chain` return "not found" when ground truth clearly has matches | `src/twin/api/twin-query-service.ts` (the underlying Cypher for these 3 ops) | Needs Cypher-level debugging with query-plan visibility I didn't have time to safely acquire without risking a bad edit to a widely-shared query file; each is an independent root cause, not a single fix. Concrete repro queries are in the corpus for a future session. |
| `A-OP-02`/`A-OP-09`: "list aliases" and "routing table" questions get answered with firewall-rules-dump / interface-list respectively | Tool-selection in the EXECUTE-path LLM loop, likely `system-prompt.ts` tool guidance for `opnsense_readonly` | LLM tool-choice issue, not a deterministic code bug — would need prompt-engineering + a larger corpus to validate a fix doesn't regress other opnsense_readonly routing. |
| `A-TQ-22`, `A-TQ-21`/`A-TQ-23`: exposure questions scoped to one VM return the *entire* exposure map (self-contradictory in `A-TQ-22`'s case: says "None VMs exposed" then lists several) | `exposure_vm_analysis`/`exposure_vms_by_subnet` op formatting in `TwinQueryTool.ts` or the twin-query-service equivalent | Real bug, but the exact formatting logic needs a closer read than time allowed this session; flagging with a strong repro is more valuable than a rushed partial fix. |
| `B-06`: `"what is enx000ec698587a?"` (generic phrasing, no "network interface" framing) gets a hallucinated description instead of real twin data, vs. `B-05`/`B-07` (with "network interface" framing) which now work correctly | `detectNetworkIntent.ts` — the phrase-matching for MAC-style `enxXXXXXXXXXXXX` names is narrower than the "what is X" generic phrasing | Partial fix already landed upstream (this campaign confirmed `B-05`/`B-07` now work); the remaining phrasing gap is a smaller, separate regex extension similar to fix #3 above — good next-session candidate. |
| `B-09`: `"what's the uptime of WindowsVM?"` (mixed case) returns `uptime=unknown` while lowercase/correct-case return the real value | LLM tool-choice variance (sometimes picks `get_vm_config` instead of an uptime-bearing op) | Not a deterministic code bug — same underlying VM is found correctly (case-insensitivity itself works, contrary to the original expectation that this was still broken); only the *specific metric* returned varies by LLM day. |
| `F-02`: *"List all nodes and their exposure level."* → *"Which VMID is this for?"* | LLM picks a single-VM-scoped `exposure_vm_analysis` tool call for a cluster-wide question | Same class as `A-OP-02`/`A-OP-09` above — tool-choice, not code. |
| `F-04`: node VM-count comparison across 3 nodes returns wrong counts (yang shown as 2, actually 5; yin shown as 0, actually 3) | LLM aggregation across multiple tool calls in the EXECUTE path | Inherent to `gpt-4o-mini` multi-tool synthesis without a structured aggregation step; not fixable with a small deterministic patch. |
| `F-06`: raw pfctl rule syntax (`"scrub in all fragment reassemble"`, `"block drop in log on ! vtnet1..."`) dumped unformatted into a composite exposure+firewall answer | EXECUTE-path formatting for a `proxmox_readonly`/`opnsense_readonly` composite call, likely bypassing `applyAdaptivePackaging` because the intent isn't classified as `firewall_rules` | Needs the same kind of `intentType` plumbing fix #5 touches, but for a different call site; deferred to keep this session's diff small. |
| `H-05`: a Cypher-injection-flavored VM name (`"' MATCH (n) DETACH DELETE n //"`) caused the agent to attempt a real `compute.destroy_vm` action against **`windowsVM`** specifically (caught safely by the pre-existing Terraform-state check: *"was not present in Terraform's destroy plan"* — no actual damage) | Classification picking up on the literal word `DELETE` in the adversarial string | **Flagging prominently for TJ's judgment call, not attempting a fix.** The safety net held (no VM was actually touched), but the fact that injection-flavored text containing `DELETE` gets classified as a genuine destroy-VM *action* attempt — and that it silently substituted an unrelated real VM name (`windowsVM`) rather than the (nonexensistent) name in the injection string — deserves a closer security-flavored look before deciding what "correct" behavior should be here. |
| `G-08`: user says "call me Ripley", agent confirms; very next turn "what's my name?" gets "I don't have your name yet" | `src/agent/handlers/handle-identity.ts` / `identity-helpers.ts` — `extractUserNameUpdate` sets it, but the persisted `conversationContext` apparently isn't read back correctly (or the accepted-phrasing set for the *query* path is narrower than for the *update* path) | Reproducible, real bug, but identity/session persistence touches server-side conversation-context storage (`src/pce/api/server.ts`) that's riskier to patch quickly without a wider test pass; flagged for a dedicated session. |
| `G-01`/`G-02`/`G-04`: multi-turn clarification-continuation and entity-resolution-cache are inconsistent — sometimes "YANG" after a clarifying question correctly continues the create-VM flow (per `docs/observability-reasoning-traces.md`'s worked example), sometimes it doesn't and the agent asks a second, unrelated clarifying question | `src/agent/handlers/clarification-continuation.ts` + entity cache wiring in `handle-execute.ts` | This is exactly REVIEW.md §8/Gap 2 ("Re-Classification Tax") — a known, called-out architectural gap requiring the typed inter-turn state REVIEW.md recommends, not a small patch. Roadmap, not a bug to spot-fix. |
| `C-01`/`C-03`: VLAN-50-reachability and port-8006-rule-attribution questions return real, correct *raw* data (switch ports; full rule list) but don't scope/synthesize it into a direct answer to the actual question asked | Chain selection in `detectNetworkIntent.ts`/`detectFirewallIntent.ts` picking a "list X" op instead of the more specific op that already exists (`switch_ports_by_vlan` for C-01; the port-8006 rule *is* in `firewall_list_rules`'s output for C-03, just not filtered/attributed) | Both are close to working — the data is right there — but scoping them correctly needs the query's specific filter (VLAN number, port number) threaded through to the right op, similar in shape to fixes #3/#4 above. Good next-session quick wins. |

### Vision-gap probes — graceful vs. broken (as instructed, not "fixed")

Per the task's framing, Phase 2/3/4 roadmap gaps are **not bugs**. Assessed:

- **Genuinely graceful** (asks a reasonable clarifying question or states the limitation, no hallucination, no crash): `C-02` (VPN→Pi-hole path), `C-05` (DNS dependents — Pi-hole not ingested), `C-09` (windowsVM-vs-sentinelZero reachability asymmetry — gives a plausible qualitative answer while implicitly working within same-subnet-only reachability).
- **Genuinely graceful, now much better than before this session's fixes:** `C-10` (undocumented trust-zone paths) — was leaking `fromId` verbatim (fixed #6), now cleanly hits `MAXSTEPS` instead.
- **Not broken, but not "graceful" either — a residual routing gap** (real data exists and is close, just not scoped to the question): `C-01`, `C-03` (see table above).
- **Working correctly** (Phase 0/1 capabilities, confirmed live): `C-06` (switch trunk port → correctly identifies `Gi0/33`/`Gi0/34`), `C-07` (WAN exposure inventory), `C-04` (blast-radius qualitative answer for "yin goes down").

---

## Phase 6 — Fixes shipped (uncommitted, for review)

| File | Change | Test added |
|---|---|---|
| `src/agent/tool-executor.ts` | try/catch around `tool.execute()` | `tests/agent/tool-executor.test.ts` (new) |
| `src/agent/handlers/handle-execute.ts` | defer failure-reclassification `addUserMessage()` until the full tool_calls batch has responses | *(see "what I did not unit-test" above; verified live)* |
| `src/reasoning/intent-router.ts` | expanded `isClearInformationalQuery()` patterns | `tests/reasoning/classify-and-route-conversation.test.ts` (extended) |
| `src/reasoning/detectFirewallIntent.ts` | fixed `extractAliasName()` greedy capture + preceding-token support + impact-question guard | `tests/reasoning/detect-firewall-intent.test.ts` (extended) |
| `src/agent/response-formatter.ts` | `normalizeCountPackaging()` no longer destructively truncates on failed extraction | `tests/agent/response-formatter-adaptive.test.ts` (extended) |
| `src/tools/AskMissingTool.ts` | added plain-English mappings for `fromId`/`chain`/`subnet`/`ruleId`/`alias` slots | `tests/tools/ask-missing-tool.test.ts` (new) |

**Verification run:**
- `bun run --bun tsc --noEmit` — clean, both before and after every change.
- `bun test` (full suite, twice — once with fixes, once on a clean stash as baseline) — **640 pass / 66 fail / 11 skip** with fixes vs. **629 pass / 71 fail / 11 skip** on baseline. Diffed the two failure-name sets directly: every failure in the "with fixes" run is either (a) an environment-dependent test needing live Proxmox/OPNsense credentials not configured in this sandbox (`TL-2A.1`/`TL-2A.2`/`TL-2A.4`/`TL-2A.6.A` suites — present in both runs, identical), or (b) already-known-flaky live-LLM-call tests (`runner-confirmation-flow.test.ts`, `runner-mvs.test.ts`, `phase-ic-dod.test.ts`) — confirmed flaky **independent of my changes** by running each in isolation 2-3x and observing different specific sub-tests fail each time (matches REVIEW.md §14.3 P0.2's documented, still-open "full-suite stability under concurrent Bun load" issue). **Zero new deterministic regressions** attributable to this session's diff.
- My 6 fixes' own test files (24 new/extended test cases across `tool-executor.test.ts`, `ask-missing-tool.test.ts`, `classify-and-route-conversation.test.ts`, `detect-firewall-intent.test.ts`, `response-formatter-adaptive.test.ts`) each independently confirmed to **fail on the pre-fix code** (via `git stash push -- <file>` + rerun) and **pass on the fixed code**.

---

## Phase 7 — Live re-verification

Ran a dedicated 16-query re-verification corpus (`fuzz-reverify-corpus-2026-07-21.json`) against the live stack **twice**: once immediately after landing fixes #1/#3/#4/#5/#6 (before fix #2 was discovered — 3/16 still crashed), and again after landing fix #2 and a full stack restart (0/16 crashed). Stack was restarted cleanly both times (`pc-stacks up palindrome` after a `docker compose down` to clear a stale network from the abrupt earlier kill) with twin data confirmed intact via direct Cypher check each time (Neo4j uses a named volume, survives container recreation).

| Query | Pre-fix (original run) | Post-fix (final live re-verify) |
|---|---|---|
| "Give me a summary of the whole cluster." | Generic "observe/diagnose/change?" clarification | ✅ Full cluster summary with per-node VM counts + memory usage |
| "Ping 172.16.0.1 for me." | Same generic clarification | No longer clarification-looped; now a (separate, unfixed) tool-declined response — **routing bug fixed, secondary tool-choice issue remains** |
| "Traceroute to 8.8.8.8." | Same generic clarification | No longer clarification-looped; now `MAXSTEPS` after 4 real tool attempts — **routing bug fixed, secondary tool-arg issue remains** |
| "Run a full health diagnostic on windowsVM." | Same generic clarification, *then* falsely claimed no info existed for windowsVM | No longer clarification-looped; now `MAXSTEPS` after 12 real tool attempts (was previously giving up after 1) — **clear improvement, not fully resolved** |
| "vms???? on yang???? pls?????" | Same generic clarification | ✅ Correctly lists YANG's VMs |
| "What's in the WG_VIP alias?" | (Not tested — new capability) | ✅ Correctly resolves via new preceding-token support: `Alias WG_VIP contains one entry: 10.16.0.0/29` |
| "What would break if I deleted the Home_DNS alias?" (control) | ✅ Reasonable EXECUTE-path answer | ✅ Unchanged — confirms no regression from the alias-impact guard |
| "What VMs would be affected if the WG_VIP alias were removed, and are any of them internet-exposed?" | `"No alias named 'were removed, and are any of them internet-exposed' was found"` | ✅→`MAXSTEPS` across the two re-verify passes (first pass got a fully correct, grounded answer citing the real `10.16.0.0/29` CIDR; second pass hit the step budget mid-reasoning) — **the garbled-name bug is gone either way; remaining variance is inherent to a genuinely hard 2-part composite question** |
| "List all firewall aliases and how many entries each one has." | `"Firewall Rule Count"` (all data silently discarded) | ✅ `"Firewall Rule Count - total \| Count=104 \| PASS=71 \| BLOCK=24 \| NAT=9"` — **destructive truncation confirmed fixed**; semantic mis-routing (counts rules not aliases) is the separate, documented residual finding |
| "Which assets have an undocumented path between the home and lab trust zones?" | `"What is the fromId for the asset in question?"` (raw param leak) then, on reproduction, **CRASH** | No param leak; `MAXSTEPS` — **both issues resolved** |
| "Can something on 172.16.0.0/22 reach 192.168.68.0/22?" | **CRASH** (reproduced 2/2 in isolation before fix #2) | **0/1 crash** after fix #2 — `MAXSTEPS` instead |
| "What disks are attached to proxBig?" | **CRASH** | ✅ Full disk data returned |
| "What's the system status of the OPNsense box?" | **CRASH** | `MAXSTEPS` (no crash) |
| "List every network interface on every node." | **CRASH** | ✅ Full interface data returned |
| "For every stopped VM, tell me which node it's on and whether any firewall rule references it." | **CRASH** | `MAXSTEPS` (no crash) |

**Crash tally: 7/16 → 0/16.** No fix regressed a previously-passing case.

**Disposable-VM lifecycle test:** not performed. None of the 6 fixes required live compute-provisioning to verify (all are read-path/formatting/classification fixes); per the task's own framing this sub-task was optional and scoped to "only if you have a specific fix that needs live compute-provisioning verification."

**Dashboard-level confirm-button check (`B-04-dashboard`):** verified at the code level (`dashboard/js/chat.js`'s `pendingConfirmationActive` guard from commit `bcfe65d` is present and matches the described fix) and at the API level (`AWAITING_CONFIRMATION` state + `CONFIRM <id>`/`CANCEL` prompt returned correctly, both in the original fuzz run and in a fresh live check). Did not additionally drive a headless browser against `dashboard:serve` — the task marked this as one of "2-3 representative cases, not required for all," and the code+API evidence is unambiguous for this specific, narrowly-described bug.

---

## What I chose not to fix, and why (summary)

- **Roadmap gaps** (Phase 2/3/4 per `lvl3-vision-gap-plan.md`): NAT/route firewall edges, real reachability evaluation, Pi-hole ingestion. Confirmed these degrade gracefully rather than break, per the task's explicit instruction not to build roadmap capability in this pass.
- **LLM tool-choice variance** (`A-OP-02`, `A-OP-09`, `F-02`, `F-04`, `B-09`): not deterministic code bugs; fixing would mean prompt-engineering with a much larger validation corpus than one session allows, risking regressing other tool-choice behavior.
- **Multi-turn conversational-state architecture** (`G-01`, `G-02`, `G-04`): exactly REVIEW.md's flagged, large, deliberately-out-of-scope architectural gap (typed inter-turn state).
- **`G-08` identity persistence, `A-TQ-10`/`16`/`19` twin-query op bugs, `A-TQ-21`/`22`/`23` exposure-scoping, `F-06` raw-pf-dump formatting, `C-01`/`C-03` scoping, `B-06` MAC-lookup phrasing gap:** real, reproducible, independently-scoped bugs — each would be a reasonable, small, correct diff on its own, but doing all of them properly (with tests) in this session would have meant less rigor on the ones I did fix and verify live. Prioritized the highest-blast-radius fixes (2 crash root causes affecting ~7 distinct queries + 1 routing bug affecting ~6 queries) over breadth.
- **`H-05`** (injection string → real destroy_vm attempt against an unrelated VM, safely caught by Terraform-state check): flagged for TJ's judgment call rather than a unilateral fix, since "what should classification do with adversarial text containing `DELETE`" is a product/security decision, not just a bug.

---

## 2026-07-21 Follow-up session — B-06 domain-gate fix + MAX_STEPS policy

**Scope:** two independent tasks against the live stack at `b8cdc34` (already includes the twin-query Cypher fixes, `alias_list`, port-8006 attribution, and the MAC-interface `interfaceLookupChain` wiring from that commit). Files touched: `src/agent/runner.ts`, `src/agent/handlers/handle-execute.ts`, `src/pce/api/reasoning-trace-store.ts` (one-line type addition), plus new tests. A parallel worker was fixing `G-08`/`F-06`/`C-01`/the `H-05` residual at the same time in different files — no overlap.

**Environment note (read before trusting any "before" numbers below):** the `pce-api` process that was "up and healthy" at session start turned out to predate `b8cdc34` by ~2.5 hours (started before `2d7b38f`/`9c29f44`/`78395c2`/`b8cdc34` even landed) — `pc-stacks up palindrome` only starts a fresh process if `.palindrome-service.pid` isn't already alive, so a stale process silently survives across sessions. My first B-06 repro against that stale process showed a *different*, more confusing symptom (a real EXECUTE-path tool-calling flail, not the documented "0 tool calls" hallucination) purely because the stale code didn't have the MAC-regex detector or `interfaceLookupChain` wiring at all yet. Restarting picked up current source and reproduced the documented symptom exactly. **Restarting the API process to load current source before trusting a live "before" measurement is not optional** — flagging this prominently since it cost real time to diagnose and would silently invalidate re-verification otherwise.

A second environment hazard, unrelated to the app code: recreating the `qdrant`/`neo4j`/`ollama` containers (via `pc-stacks up` or `docker compose up -d`) intermittently swept in the repo's one-off `clear-vector-store` service (defined in `docker-compose.yml`, meant for manual `docker compose run --rm clear-vector-store` only, `restart: "no"`) and ran it, silently deleting the `pce_documents` Qdrant collection. Hit this twice, recovered both times via `bun run pce:ingest-all` (Neo4j data was never affected — separate named volume, confirmed via direct Cypher count each time). **Not a code bug in this repo's application layer** and out of scope for this task's file list, but worth a `docker-compose.yml` follow-up (e.g. a profile gate on `clear-vector-store` so a bare `up -d` never includes it) — flagged for a future session, not fixed here.

**Mid-session correction:** TJ flagged that the live `pce-api` on port 4000 and the Docker stack were shared with the parallel worker, and repeated restarts (mine, chasing the two hazards above) risked dropping their in-flight requests. Switched immediately to a fully isolated `PCE_API_PORT=4001` instance (same Neo4j/Qdrant, isolated app process) for all live verification from that point on, per TJ's instruction, and stopped touching port 4000 / the shared Docker containers entirely afterward (one remediation exception: my own now-abandoned restart attempt had transiently torn down the shared `neo4j`/`qdrant`/`ollama` containers via a failed `start-all.ts` run's cleanup-on-failure path before the correction arrived — brought them back up once, immediately, since leaving them down would have broken the other worker too; confirmed no data loss via the same Cypher-count check).

### Task 1 — B-06: domain-gate routing gap

**Root cause (confirmed live, not guessed):** `runner.ts`'s twin-first network-reasoning block (~line 2165) gated entry on `!classification.metadata?.domain || classification.metadata.domain === "network"` *before* ever calling `detectNetworkIntent()`. The LLM-based intent classifier (`gpt-4o-mini` via `generateObject`, non-deterministic) does not reliably tag bare interface-style lookups — `"what is enx000ec698587a?"` — with `domain: "network"`. Sampled 7 live classifications of that exact query on the current (fixed) build: **2/7 came back `domain: "general"`** (and one had no `domain` key at all). Whenever the domain came back anything other than `"network"`/absent, the gate skipped the entire block — `detectNetworkIntent()` was never called — and the query fell through to the EXECUTE/LLM path, which had no real handle on the MAC-style interface name and either hallucinated a generic explanation of `enx` naming (0 grounded tool calls) or burned the full tool-call budget flailing (`network_list_interfaces` → empty → guessing `vmid`/`node` combinations) before hitting `MAXSTEPS`. Live trace evidence for both failure modes is in the transcript; representative reasoning-trace decision: `{"type":"retrieval_skipped","metadata":{"reason":"tool_first_domain"}}` immediately followed by real `proxmox_readonly` tool calls — i.e. it reached `handleExecute()`, meaning the twin-first block above it never fired.

Confirmed `detectNetworkIntent()` itself was already correct for both phrasings (`"what is enx000ec698587a?"` and `"what network interface is this? enx000ec698587a"` both resolve to `{ type: "interface_lookup", interfaceName: "enx000ec698587a" }`) and that when the classifier *did* happen to say `domain: "network"`, the existing gate + `interfaceLookupChain` wiring from `b8cdc34` worked correctly end-to-end. The bug is specifically the domain-gate's blind spot for off-domain misclassifications of an otherwise-detectable query — not a detection gap, not a missing chain.

**Fix (`src/agent/runner.ts`):** extracted the gate condition into a small, pure, exported predicate, `networkDomainGateAllows(domain, networkIntentMatch)`, and widened it to also pass when `detectNetworkIntent(userInput)` returns a positive match — regardless of what domain the LLM guessed. This mirrors the existing `canHandleDirectlyFromRoute`/`ASK_CLARIFY`-bypass pattern already in the same file (lines ~1172–1213), which already trusts a positive deterministic-detector match over the LLM's own routing decision for a *different* gate (clarification) — just needed the same trust extended one gate later, for this specific block. Scoped narrowly to the network gate only, per the task's instruction not to weaken the general domain-gate mechanism: the compute/firewall/exposure gates a few lines above are untouched, and queries the deterministic detector genuinely doesn't recognize (e.g. `"install docker on yang"`) still require an absent-or-matching domain exactly as before — nothing about the gate's off-domain-suppression purpose (REVIEW.md §14 Cleanup A) changed for queries outside `detectNetworkIntent`'s scope.

**Test:** `tests/agent/network-domain-gate.test.ts` (new, 7 cases). `runAgent()`/`classifyAndRouteWithLLM()` have no dependency-injection seam (module-level OpenAI client, no way to mock the LLM classification without live calls or invasive module mocking) — consistent with the original campaign's own judgment call for this exact file, the pure gate predicate is unit-tested directly and the full wiring is verified live below. Confirms: absent/matching domain still passes (no behavior change), a genuinely off-domain query with no detector match still correctly blocks (gate not weakened), and a positive detector match now passes regardless of domain (the fix) — including the exact `"what is enx000ec698587a?"` repro string.

**Live re-verification (isolated `:4001` instance, current source):**

| Query | Sampled classifier `domain` | Before fix (documented in commit `b8cdc34`) | After fix |
|---|---|---|---|
| `"what is enx000ec698587a?"` (bare) | `general`, `general`, (absent) across 3 fresh samples | 0 tool calls, hallucinated generic `enx` explanation, OR (on stale code) a full tool-flail ending in MAXSTEPS | ✅ 6/6 across two verification passes (3 pre-correction on :4000, 3 post-correction on :4001): `"Network Interface\n- enx000ec698587a \| node=proxBig \| status=up"`, `totalToolCalls: 0` (twin-first short-circuit, not an EXECUTE-path tool call), `intent: "interface_lookup"` |
| `"what network interface is this? enx000ec698587a"` (framed) | `network` in all 3 samples (this framing reliably classifies correctly) | not re-confirmed in the prior session (SSE didn't return in time) | ✅ 6/6 across both passes, identical grounded answer to the bare phrasing |

Exact `agent:final` payload for one bare-phrasing run (domain was `general` for this specific sample, i.e. this is a case the pre-fix gate would have skipped):
```json
{"type":"agent:final","totalSteps":0,"totalToolCalls":0,"structuredResponse":{"answer":{"summary":"Network Interface","sections":[{"type":"text","data":"- enx000ec698587a | node=proxBig | status=up"}]}},"text":"Network Interface\n- enx000ec698587a | node=proxBig | status=up"}
```

No regressions observed on the compute/firewall/exposure gates or on other network-domain queries (spot-checked `"What VMs are on yang?"`, `"List all firewall aliases"` — both still resolve via their existing twin-first chains in 1-2 steps, unaffected).

### Task 2 — MAX_STEPS policy

**Where it's defined/enforced:** `src/agent/handlers/handle-execute.ts`. `MAX_STEPS` (was a bare `const = 5`) bounds the number of LLM turns in the tool-calling `for` loop; `MAX_TOOL_CALLS_PER_STEP = 5` (unchanged) bounds parallel tool calls within one turn. At the boundary, the user sees a hardcoded `"Max reasoning depth reached. Please try a simpler query."` — **all tool results and partial progress gathered during the run are discarded from the user-facing answer** (they're preserved in the reasoning trace for observability/debugging via `GET /api/dashboard/reasoning-traces/{traceId}`, but the agent never gets a chance to synthesize from them). This discard-everything boundary behavior is a real, separate finding (see Residuals below) — this session's fix targets the *budget/loop* mechanism the task scoped, not the boundary synthesis.

**Evidence gathered:** grepped `maxStepsReached":true`/`MAXSTEPS`/`Max reasoning depth` across `docs/tests/fuzz-results-2026-07-21.jsonl` (3 hits) and `docs/tests/fuzz-reverify-results-2026-07-21.jsonl` (7 hits) — 10 total distinct live MAXSTEPS hits, each with an embedded reasoning trace. Classified every one by its actual step-by-step tool-call sequence (not just the final "MAXSTEPS" label):

| Query | Pattern | Real tool calls | Classification |
|---|---|---|---|
| `A-PX-10` "What's the Ceph status on the cluster?" | tries `describe_cluster` (works), then wanders into `opnsense_readonly`/unapproved `ssh_execute ceph status` | 5/5 steps used | **Capability gap** — no Ceph twin op, no approved Ceph SSH command exists. More budget wouldn't produce an answer. |
| `A-OP-07` "Is OPNsense healthy right now?" | `uptime` succeeds, then `free -h`/`top`/`du` all fail on Linux-flavored syntax against OPNsense's BSD userland | 5/5 steps, 6 tool calls | **Tool/prompt gap** (BSD vs Linux command syntax), not a budget problem — and a case of good data (`uptime`) discarded at the boundary. |
| `F-08` "...dual-homed VMs...exposed?" | 1 real tool call, then **4 consecutive steps with zero tool calls** | 1/5 steps used | **Loop bug** — LLM returned neither a tool call nor text; nothing was appended to context, so the identical prompt just got resent for free each remaining step. |
| `RV-ROUTING-04` "Run a full health diagnostic on windowsVM." | early steps waste calls on missing params, but by step 3–5 real diagnostics start succeeding (`list_vms`, `guest_agent`, `get_vm_config`) | 12 tool calls across 5 steps, climbing | **Genuine forward progress, ran out of budget** — was converging on a real answer. |
| `RV-ALIAS-01` "...WG_VIP alias removed...exposed?" | 3 real successes in steps 1–3, then **2 consecutive empty steps** | 5 tool calls, then stalls | **Loop bug** (same empty-step pattern as `F-08`), on top of a real composite question. |
| `RV-ROUTING-03` "Traceroute to 8.8.8.8." | same failing command repeated 3× with only cosmetic param changes (`maxHops`, `timeoutMs`) | 4 tool calls, all but 1 failing identically | **Thrashing** — deterministically-repeated identical failure. More budget = slower, costlier identical failure. |
| `RV-CRASH-01` "Can something on 172.16.0.0/22 reach 192.168.68.0/22?" | 2 real successes interleaved with 2 empty steps | 3 tool calls | **Loop bug + genuine hard composite question** (no direct twin op for subnet-to-subnet reachability). |
| `RV-ASKMISSING-01` "...undocumented path between home/lab trust zones?" | 3/5 steps succeed (`exposure_map`, `rules_allowing_subnet`, `rules_blocking_subnet`), gathering real, relevant data | 5/5 steps used | **Genuine forward progress, ran out of budget** — had real data, needed to synthesize. |
| `RV-CRASH-03` "What's the system status of the OPNsense box?" | `uptime` **and** `df -h` both succeed by step 5 — real, sufficient data — but budget ran out right after | 5/5 steps, 5 tool calls | **Good data discarded at the boundary**, not a budget-size problem (a 6th step would just be a synthesis call, not more searching). |
| `RV-CRASH-05` "For every stopped VM...which node...firewall rule references it?" | real progress for 1/3 nodes by step 3, then 2 empty steps | 4 tool calls | **Loop bug + genuinely needs to iterate more nodes** (composite/iteration shape). |

Tally: **2/10 capability gaps** (no fix helps), **1/10 thrashing** (fix = fail faster/cheaper, not fail less), **4/10 the empty-step loop bug** (F-08, RV-ALIAS-01, RV-CRASH-01, RV-CRASH-05 — a real, mechanistic bug, not a budget-size question), **2/10 genuine forward progress cut short by a flat 5-step ceiling** (RV-ROUTING-04, RV-ASKMISSING-01), **1/10 (RV-CRASH-03) genuinely-sufficient data discarded at the boundary** (some queries land in more than one bucket).

**The empty-step loop bug, mechanistically confirmed:** when the LLM's response has zero `tool_calls` and empty/blank `content`, the loop's existing code does nothing — no `context.addAssistantMessage()`, no `context.addUserMessage()`, nothing added to the conversation array — before looping again. The *next* iteration therefore re-sends the model an **unchanged** prompt. This is not "the model is thinking it over for 4 more turns"; it's the exact same input being replayed, wasting the entire remaining step budget for zero possible benefit regardless of how large `MAX_STEPS` is. This alone explains 4 of the 10 evidenced MAXSTEPS hits and would not have been fixed by just raising the constant.

**Decision:** neither a pure "raise the constant" nor "leave it at 5" fits the evidence — a flat raise would make the capability-gap and thrashing cases (3/10) slower and more expensive for the exact same eventual failure, while doing nothing for the loop-bug cases (4/10, the single largest bucket) since more of an unchanged prompt doesn't help either. Implemented two complementary, evidence-targeted, independently-simple mechanisms instead:

1. **Fix the empty-step loop bug directly** (`isStuckOnEmptyStep()` + a one-time corrective nudge). When a step produces neither a tool call nor text: inject a plain system-style nudge (`"You did not call a tool or provide an answer..."`) into context so the *next* attempt has a genuinely different prompt, then continue. If it happens **twice in a row**, stop immediately — same final user-facing message ("Max reasoning depth reached"), reached faster and cheaper, since a 3rd identical retry has no evidence of helping. `EMPTY_STEP_STUCK_THRESHOLD = 2`.
2. **Scale the budget up only for query shapes that evidence shows genuinely need more than 5 turns** (`computeMaxSteps()`): composite (multi-dimension) queries, "all nodes" sweeps, explicit "full/complete/comprehensive diagnostic" requests, and "for every/each X" iteration-over-entities questions → `EXTENDED_MAX_STEPS = 8`. Everything else — the common case, and the thrashing/capability-gap cases — keeps `BASE_MAX_STEPS = 5` exactly as before, so ordinary single-answer queries see zero latency/cost change and already-doomed queries don't get slower.

Deliberately **not** implemented this session (flagged as residuals, same rigor as the original campaign's "found, prioritized, not fixed" table):
- **Boundary-synthesis fallback** (make one final tool-free LLM call to synthesize from whatever was gathered, instead of the canned message, when at least one tool call succeeded) — would fix `RV-CRASH-03` and improve `A-OP-07`, but is a different mechanism than "the step budget," has no clean mock-free test seam of its own, and risks scope creep on a shared file. Good next-session candidate; the evidence for it (2/10 cases with real, sufficient, discarded data) is now documented above.
- **Duplicate-call thrashing** (`RV-CRASH-05`/`F-08`'s residual pattern, see below) — a related-but-distinct stall where the LLM *does* emit `tool_calls`, but they're exact repeats of already-tried calls, silently filtered by the existing `seenToolCalls` dedup (which correctly prevents wasted tool executions) while still costing a full empty *step* each time. `isStuckOnEmptyStep()` doesn't fire here because the raw LLM response had a nonzero `tool_calls` count — confirmed via live trace inspection below.

**Implementation:** `src/agent/handlers/handle-execute.ts` — `BASE_MAX_STEPS`/`EXTENDED_MAX_STEPS`/`EMPTY_STEP_STUCK_THRESHOLD` constants, `computeMaxSteps()`, `isStuckOnEmptyStep()` (all exported, pure functions), plus the loop wiring (empty-step counter, nudge, early-break). `src/pce/api/reasoning-trace-store.ts` — added an `"empty_step"` decision type so the new nudge/stuck decisions show up in reasoning traces for observability, matching every other loop decision already recorded there.

**Test:** `tests/agent/max-steps-policy.test.ts` (new, 10 cases). Same test-seam judgment call as Task 1 and the original campaign's precedent for this file: `computeMaxSteps()`/`isStuckOnEmptyStep()` are pure and unit-tested directly (covering the base/extended budget decision for every query shape in the evidence table above, plus the stuck-threshold boundary); the full loop wiring (the nudge message, the early-break) has no mock-free seam and is verified live below. Searched `tests/` for `MAX_STEPS`/`maxSteps`/`maxStepsReached` first — the only existing hit (`tests/agent/event-bus.test.ts`) uses an unrelated arbitrary fixture value and doesn't assert on the actual constant; nothing needed preserving/updating.

**Live re-verification (isolated `:4001` instance)** — reran 6 of the 10 evidenced MAXSTEPS queries plus 3 previously-fast queries (regression check):

| Query | Before (documented) | After |
|---|---|---|
| `RV-ROUTING-04` "Run a full health diagnostic on windowsVM." | MAXSTEPS after 12 tool calls, still climbing | ✅ **Completes** — full diagnostic answer (guest agent status, network config per-interface, recommendations). `totalSteps: 5, totalToolCalls: 5` |
| `RV-ALIAS-01` "...WG_VIP alias removed...exposed?" | MAXSTEPS after 5 tool calls (empty-step stall) | ✅ **Completes** — real answer (`affected_vms: none`, `exposed_vms: none`, resolved alias CIDR `10.16.0.0/29`). `totalSteps: 4, totalToolCalls: 5` |
| `RV-ROUTING-03` "Traceroute to 8.8.8.8." | MAXSTEPS after 4 tool calls (thrashing on an identically-failing command) | ✅ **Completes** — real 13-hop traceroute result. `totalSteps: 4, totalToolCalls: 3` (base budget, unextended — the underlying command simply succeeded this time; no regression either way) |
| `RV-CRASH-05` "For every stopped VM..." | MAXSTEPS after 4 tool calls | Still MAXSTEPS, but **measurably more real progress**: `totalSteps: 8` (extended budget engaged), `totalToolCalls: 9` (up from 4). Trace shows the *duplicate-call* stall pattern (LLM re-suggesting already-tried calls, filtered by `seenToolCalls`) on 3 of the 8 steps — the residual documented above, not the empty-step bug this session fixed. |
| `F-08` "...dual-homed VMs...exposed?" | MAXSTEPS after 1 tool call (severe empty-step stall) | Still MAXSTEPS, but **7× more real tool activity**: `totalSteps: 8` (extended, composite-flagged), `totalToolCalls: 7` (up from 1). Same duplicate-call residual pattern on 3 of 8 steps. |
| `RV-CRASH-03` "What's the system status of the OPNsense box?" | MAXSTEPS after 5 tool calls (good data discarded) | Unchanged outcome (MAXSTEPS), as predicted — `totalSteps: 5` (correctly *not* extended; not composite/all-nodes/diagnostic-phrased), `totalToolCalls: 7`. This is the documented boundary-synthesis residual, not something this session's fix addresses. |

**No regressions:** 3 previously-fast queries re-checked (`"What VMs are on yang?"`, `"What is the uptime of pihole?"`, `"List all firewall aliases"`) — all still resolve in 1–3 steps, 6–12 seconds, correct data, `computeMaxSteps()` correctly keeps them at the base budget (unextended).

**Net effect:** 3/6 previously-MAXSTEPS queries now complete with real, grounded answers; 2/6 make substantially more real progress (7–9× more tool calls) but still don't finish (a distinct, now-documented residual: duplicate-call stalling, separate from the empty-step bug this session fixed); 1/6 is an unrelated, already-documented boundary-synthesis gap, correctly unaffected. Zero regressions on fast-path queries.

**Residuals — repro evidence for a future session:** the two non-"complete" outcomes above are distinct failure modes from each other and from anything fixed this session. Neither was patched here — evidence below, so a future session can pick either up directly instead of re-discovering it.

*Duplicate-call pattern (`RV-CRASH-05`, `F-08`).* Root cause, confirmed directly from source (`handle-execute.ts`): the existing `seenToolCalls` dedup (lines ~1196–1216) already recognizes when the LLM re-proposes a call it already made and records a `duplicate_detected` decision — visible pre-fix in both traces below — but a step where *every* proposed tool call gets filtered as a duplicate still consumes a full step for zero new information. This is functionally the same failure as the empty-step bug fixed this session, except `isStuckOnEmptyStep()`'s counter (`consecutiveEmptySteps`, lines ~1943–1966) only increments on the branch where the raw LLM response has **no** `tool_calls` at all — a response that's 100% duplicates never reaches that branch, so this session's stuck-detection doesn't cover it.

| Query | Pre-fix raw trace (in-repo, inspectable) | Pattern observed |
|---|---|---|
| `RV-CRASH-05` "For every stopped VM, tell me which node it's on and whether any firewall rule references it." | `docs/tests/fuzz-reverify-results-2026-07-21.jsonl:15`, traceId `ea530bfc-130b-4858-90ad-507bf40d7632` | Steps 1–3 make real progress (`firewall_exposure_map`, `list_all_vms`, `vms_by_node YANG` all succeed), with `duplicate_detected` already appearing alongside real calls by step 3; steps 4–5 are **100% `duplicate_detected`, zero real tool calls**. Post-fix (Task 2 table above; live-verified, not persisted as raw JSON): `totalSteps: 8, totalToolCalls: 9` (up from 4) — 3 of the 8 steps still show the identical stall. |
| `F-08` "Which nodes have VMs on both the home and lab subnets, and are any of those dual-homed VMs exposed?" | `docs/tests/fuzz-results-2026-07-21.jsonl:117`, traceId `26782bcb-9f17-4573-ac55-fc935b70a204` | Step 1's only real call (`network_vms_by_subnet subnet="home"`) returns an empty list; steps 2–5 are **100% `duplicate_detected`, zero real tool calls** — the LLM just keeps re-proposing the same already-empty query. Post-fix: `totalSteps: 8, totalToolCalls: 7` (up from 1) — same pattern on 3 of 8 steps. |

Suggested fix direction (not attempted here, to keep this session's diff scoped to the mechanism actually root-caused): extend the stuck-detection to also count a step whose tool calls were *entirely* filtered as duplicates — the trace already carries everything needed (`duplicate_detected` decisions with no accompanying successful `tool_choice` execution that step), it just isn't consulted by `isStuckOnEmptyStep()`/`consecutiveEmptySteps` yet.

*Boundary-synthesis gap (`RV-CRASH-03`).* Not a regression — `computeMaxSteps()` correctly leaves this query on the base budget (it isn't composite/all-nodes/diagnostic-phrased), so this is the pre-existing, separately-scoped gap named above under "Deliberately not implemented this session," unaffected by design. Pre-fix raw trace: `docs/tests/fuzz-reverify-results-2026-07-21.jsonl:14`, traceId `31b94da9-3d8b-44b4-aada-803b6bf4ad08`, query "What's the system status of the OPNsense box?" — no dedup involved at all here; all 5 steps are genuinely distinct real calls: `opnsense_readonly system_status` (fails, endpoint unsupported) → `ssh_execute uptime` (**succeeds**: `"up 59 days, 17:52..."`) → `ssh_execute free -h` (fails, BSD has no `free`) → `ssh_execute top -b -n 1 | head -n 10` (fails, not an approved command) → `ssh_execute df -h` (**succeeds**: real disk-usage table). Budget runs out immediately after the second success, with two good results already in hand and no step left to turn them into an answer — final response is the canned `"Max reasoning depth reached."`, discarding both.

Suggested fix direction: the boundary-synthesis fallback already named above — one final tool-free LLM call synthesizing from whatever tool results exist, gated on "at least one call this run succeeded," fired instead of the canned message when `MAX_STEPS` is hit. This trace is a clean, minimal case to validate that fix against: two successful, genuinely answerable results (`uptime` + `df -h`) sitting unused.

**Repro recipe for either residual:** same pattern this campaign used throughout — `pc-stacks up palindrome` → `POST /api/agent/query {"query": "<text above>", "sessionId": "<unique>", "userId": "<unique>"}` → `GET /api/agent/stream?sessionId=<same>` (SSE) → read the `agent:final` event's `traceId` → `GET /api/dashboard/reasoning-traces/{traceId}` for the full step-by-step trace. All 3 queries are already corpus entries (`RV-CRASH-03`/`RV-CRASH-05` in `fuzz-reverify-corpus-2026-07-21.json`, `F-08` in `fuzz-corpus-2026-07-21.json`), so `scripts/fuzz-campaign-runner.ts` can drive them directly, e.g. `FUZZ_CORPUS_PATH=docs/tests/fuzz-reverify-corpus-2026-07-21.json bun run scripts/fuzz-campaign-runner.ts <outPath> 1 RV-CRASH-05`.

### Verification gates

- `bun run --bun tsc --noEmit`: clean.
- `bun test` (full suite): **726 pass / 67 fail / 11 skip** (804 total). Baseline at `b8cdc34` was documented as 694/66/11; the +32 passes are this session's 17 new test cases (`network-domain-gate.test.ts` + `max-steps-policy.test.ts`) plus the parallel worker's own new tests running concurrently in the same tree. Diffed the 67 failing test names against the documented baseline categories: **all 67** fall into the pre-existing `TL-2A.1`/`TL-2A.2`/`TL-2A.4`/`TL-2A.6.A`/`TL-1A.5` (live Proxmox/OPNsense credentials not configured in this sandbox) or known-flaky-live-LLM (`runner-mvs.test.ts`) buckets — zero failures in any file this session touched or added. Spot-checked flakiness directly: `runner-mvs.test.ts` passed 3/3 in isolation (fails only under full-suite concurrent load, matching REVIEW.md §14.3 P0.2's documented issue); `tests/tools/proxmox/config.test.ts`'s "Proxmox credential pairing" test also passed cleanly in isolation (env-var cross-test interference under full-suite load, not a regression); `tests/tools/proxmox/readonly/vector-ingestion.test.ts` failed in isolation too, confirming it's a genuine missing-live-credentials case, not something introduced here. **Zero new deterministic regressions attributable to this session's diff.**
- `.pce-dod-test/snapshots.json` / `.pce-ib-dod-test/snapshots.json`: reverted after the full test run regenerated them with fresh timestamps.
- Scratch scripts (`scripts/_debug-b06.ts`, `_debug-b06-verify.sh`, `_debug-maxsteps.ts`, `_debug-maxsteps-verify.sh`) created and used for live verification during this session, deleted afterward.

---

## Addendum — 2026-07-21 follow-up session (G-08, F-06, C-01, H-05 residual)

**Branch:** `realAgent` @ `b8cdc34` (HEAD at session start, clean).
**Scope:** the 3 highest-priority items left in the "not fixed" table above (`G-08`, `F-06`, `C-01`), plus the optional `H-05` residual-tightening follow-up from commit `78395c2`.
**Concurrency note:** this session ran **in parallel** with another worker fixing the B-06 domain-gate routing bug and the MAX_STEPS policy in `src/agent/runner.ts` / `src/agent/handlers/handle-execute.ts` on the same shared working tree and live stack. Per an explicit operational correction mid-session, all live re-verification in this addendum was run against an **isolated PCE API instance on port 4002** (`PCE_API_PORT=4002 bun run src/pce/api/main.ts`, same Neo4j/Qdrant/Ollama backing data as the shared `:4000` instance) rather than the shared port `:4000` instance, to eliminate any restart race between the two concurrent sessions. `src/agent/handlers/handle-execute.ts` was touched by both workers; the edit from this session is a minimal, clearly-delimited 7-line addition (one import, one function call with a comment referencing F-06) that survived the other worker's much larger MAX_STEPS diff untouched — see the "Files changed" table below for the exact hunk.

### G-08 — identity persistence: **not a code bug; root-caused to a test-harness/repro gap**

**Investigated:** `src/agent/handlers/handle-identity.ts`, `identity-helpers.ts`, `src/pce/api/server.ts`'s conversation-context load/save path (`handleAgentQuery`, lines ~1870-2100), `src/pce/api/chat-history-store.ts`'s `getConversationContext`/`setConversationContext`.

**Root cause:** there isn't one, in production code. `POST /api/agent/query` reads `body.conversationId` (falling back to `null`) to decide which conversation's context to load; if it's omitted, a **brand-new, context-free conversation is created every time**, regardless of `sessionId`/`userId` reuse — `sessionId` is a per-run/SSE-stream identifier, not the persistence key. This is exactly how the real dashboard client (`dashboard/js/chat.js` lines ~1584-1603) already behaves: it captures `conversationId` from the first response and forwards it on every subsequent request. The original campaign's `G-08` repro (`scripts/fuzz-campaign-runner.ts`'s `runMultiTurnFlow`) — and this task's own suggested repro command — both only reused `sessionId` across turns and never forwarded `conversationId`, so "turn 2" was silently a different, empty conversation from "turn 1" every time.

**Live evidence (isolated `:4002` instance, `PCE_API_PORT=4002`):**
| Call | `conversationId` used | Result |
|---|---|---|
| Turn 1: `POST /api/agent/query {query: "call me Ripley", sessionId: S, userId: S}` | (new, server-generated) `14a45f47-...` | `conversationContext: {"userName":"Ripley"}` ✅ |
| Turn 2 (repro as originally specified): `POST /api/agent/query {query: "what's my name?", sessionId: S, userId: S}` — **no `conversationId`** | (new, server-generated) `01f30f4d-...` — a **different** conversation | `"I don't have your name yet."` — reproduces the original finding exactly |
| Turn 2 (corrected): same query, **with** `conversationId: "14a45f47-..."` from turn 1 | same `14a45f47-...` | `"Your name is Ripley."` ✅ |

Also verified at the store layer directly (no live server needed): `ChatHistoryStore.setConversationContext()` → `getConversationContext()` round-trips `userName` correctly for the same `conversationId`, and two separate `createConversation()` calls (simulating "same user, no `conversationId` threaded") never leak context between them — see `tests/pce/api/chat-history-conversation-context.test.ts`.

**Fix:** none needed in `handle-identity.ts` / `identity-helpers.ts` / `server.ts` — the persistence contract already works correctly and matches the real client's behavior. Fixed the actual defect instead: `scripts/fuzz-campaign-runner.ts`'s `runMultiTurnFlow` now captures each turn's response `conversationId` and forwards it on the next turn's request, so future multi-turn campaign runs don't misreport this class of false positive again (this likely also explains at least part of the `G-01`/`G-02`/`G-04` "inconsistent multi-turn continuity" findings from the original report, though those remain out of scope here as originally framed — REVIEW.md's typed-inter-turn-state gap is still real and separate from this specific harness bug).

**Test:** `tests/pce/api/chat-history-conversation-context.test.ts` (new; 3 cases, no live infra — direct `bun:sqlite`-backed `ChatHistoryStore` instance per test).

### F-06 — raw pfctl dump formatting: **fixed**

**Investigated:** `src/agent/response-formatter.ts` (`applyAdaptivePackaging`, `detectResponseIntent`, the private `parseFirewallRuleEntries`), `src/agent/handlers/handle-execute.ts`'s EXECUTE-path finalization (`finalText` construction, ~line 1700-1860), `src/reasoning/chains/firewall.ts`'s `parseFilterRule`/`formatRuleList` (the twin-first path's raw-pf-to-readable pipeline).

**Root cause:** the EXECUTE path's finalization **never calls any response formatter at all** for its main text output — `formatResponseForBot`/`applyAdaptivePackaging` are wired into the twin-first chains (`runner.ts` line ~2136-2141) and one early-return branch (`alias_contents`, `handle-execute.ts` ~538-542), but the general "LLM produced final text after N tool calls" path just uses `finalText` as-is. When the LLM chooses `opnsense_readonly`'s live `firewall_rules_list` action (an array of raw pfctl strings, e.g. `"block drop in log on ! vtnet1 inet from 192.168.68.0/22 to any"`) for a composite question and echoes that data back, nothing ever prettifies it. Even if a formatter *had* run, `parseFirewallRuleEntries` only recognizes already-pipe-formatted lines (`ACTION | dir=...`), not raw pfctl syntax — so the gap is two-layered.

**Fix (`src/agent/response-formatter.ts`, new exported `prettifyRawPfctlText()`; `src/reasoning/chains/firewall.ts`, exported the existing private `parseFilterRule`/`ParsedPfRule` for reuse; `src/agent/handlers/handle-execute.ts`, one call site):** a small, deterministic, LLM-independent post-process applied to `finalText` right when it's computed in the EXECUTE path. It finds double-quoted spans (honoring escaped inner quotes, e.g. a rule's `label \"...\"`, so a whole pipe-joined rule *array* quoted as a single string isn't truncated at the first escaped quote), splits on the model's own `" | "` join convention, and reuses the same `parseFilterRule` the twin-first firewall chain already trusts to recognize genuine `pass`/`block` rule lines — rewriting only those into the existing `ACTION | dir=... | src=... | dst=...` pipe format. A confidence gate (requires at least one real rule field beyond the bare action) prevents false positives on incidental English like `"pass the salt"`. Non-rule content (`scrub in all fragment reassemble`, prose, already-formatted lines) passes through untouched. This is intentionally *not* a general formatter-dispatch fix (didn't wire `applyAdaptivePackaging`/`formatResponseForBot` into the main EXECUTE path) — that would have a much larger blast radius across every EXECUTE-path query, not just ones that happen to echo raw pfctl.

**Test:** `tests/agent/response-formatter-pfctl-prettify.test.ts` (new; 7 cases) — the original single-quoted-rule shape, the bare/bulleted shape, a synthetic reproduction of a *second* bad-output shape found during live re-verification (see below), plus explicit false-positive guards (`"pass the salt"`, `"block him"`, plain English). Confirmed failing on pre-fix code (`prettifyRawPfctlText` doesn't exist).

**Live re-verification (isolated `:4002` instance, exact original query "What's exposed on proxBig, and what firewall rules are protecting it?", 5 total runs across both the shared-`:4000` and isolated-`:4002` instances before/after the fix):**
- **Before fix:** raw pfctl leaked verbatim, e.g. `"block drop in log on ! vtnet1 inet from 192.168.68.0/22 to any"` — reproduces the original finding exactly.
- **After fix, run A** (per-rule quoting, matching the original capture's shape): `BLOCK | dir=in | if=!vtnet1 | src=192.168.68.0/22 | dst=any` — clean.
- **After fix, run B** (a *different* bad shape the LLM produced on a later run — the entire rule array as one giant quoted, pipe-joined blob, with each rule's `label` value's quotes left backslash-escaped): also fully cleaned, e.g. `BLOCK | dir=in | proto=tcp | src=<sshlockout> | dst=(self) | port=ssh`, `PASS | dir=in | if=wireguard | src=<WG_VIP> | dst=(vtnet0:network)` — the fix (added mid-session after this second shape surfaced during re-verification) generalizes correctly.
- **After fix, run C** (JSON-array-with-commas shape): individual quoted array elements still get prettified correctly; only cosmetic `[`/`]`/`,` punctuation remains from the array framing (not raw pf syntax) — an accepted minor residual given the "smallest correct diff" scope, not a regression of the core fix.
- 0/4 post-fix live runs leaked raw pfctl syntax (one run in between hit an unrelated, pre-existing `MAXSTEPS` — not a regression, matches the report's established "worst acceptable outcome" pattern for hard composite queries).

### C-01 — VLAN-50 reachability scoping: **not reproducible; already correct on `b8cdc34`; regression-tested, not re-fixed**

**Investigated:** `src/reasoning/detectNetworkIntent.ts` (`VLAN_NUMBER_REGEX`, the `switch_ports_by_vlan` branch, and the preceding `reach`/`reachable` branch that could theoretically pre-empt it), `src/agent/runner.ts`'s network-domain gate and `executeNetworkIntent` (read-only — this file is the parallel worker's scope), `src/twin/api/twin-query-service.ts`'s `switchPortsByVlan()` Cypher, and the campaign's own ground-truth snapshot for VLAN 50 port membership.

**Finding:** the task brief's claim — *"the already-existing `switch_ports_by_vlan` twin_query operation isn't being called with `vlan=50` for this phrasing"* — does not hold up under direct inspection. `detectNetworkIntent("Can VLAN 50 reach the Proxmox management interfaces?")` already returns `{ type: "switch_ports_by_vlan", vlan: 50 }` today: the preceding `reach`/`reachable` branch only *returns* when it finds a resolvable VM name or `network-if:`/`compute-vm:` entity ID (neither present here), so it falls through harmlessly to the VLAN branch further down, which does match. This logic is unchanged since before the campaign even started (`git diff bcfe65d..HEAD -- src/reasoning/detectNetworkIntent.ts` shows only the unrelated `B-06`/`A-OP-09` hunks from `b8cdc34` — nothing touches this code path).

Cross-checked three independent sources, all agreeing:
1. **The original campaign's own raw capture** (`docs/tests/fuzz-results-2026-07-21.jsonl`, `id: "C-01"`) already shows `intent: "switch_ports_by_vlan"` and the same 19-port list as below.
2. **4 fresh live re-verification runs** (2 against the shared `:4000` instance before the port-isolation correction, 2 against the isolated `:4002` instance after) — all 4 identical: `intent: "switch_ports_by_vlan"`, returning Gi0/17-32 + Gi0/41 + Gi0/33 + Gi0/34.
3. **Ground truth** (`docs/tests/ground-truth-snapshot-2026-07-21.json`): exactly 16 ports have `accessVlan=50`, plus `Gi0/41` (mode=access/vlan=40 but `trunkVlans` includes 50 — a real dual-tagged uplink) and the two declared trunk ports `Gi0/33`/`Gi0/34` (`trunkVlans: [1,40,50]`) = **19 ports total, exactly matching every run's output.**

So the *data* returned is already correctly scoped to VLAN 50 in every observed instance — this part of the original "not fixed" table's description ("real, correct raw switch-port data... but not scoped to VLAN 50 specifically") does not match its own underlying evidence. The genuinely-accurate residual gap described later in the same table row — that the response doesn't *synthesize an answer to the reachability question itself* (it lists ports, but doesn't say whether VLAN 50 can reach "the Proxmox management interfaces") — is real, but answering that would require actual cross-VLAN Layer-3 reachability evaluation, which is explicitly out-of-scope Phase-3 roadmap work (same category as `C-02`/`C-05`, the other vision-gap probes this campaign has consistently declined to "fix" by building new capability). Doing so would also require passing the original user-question text into `switchPortsByVlanChain` — whose only call site is in `runner.ts`, this session's explicitly off-limits file.

**Fix:** none shipped (none needed) — flagging per the task's own guidance to report clearly rather than force a diff where the evidence doesn't support one. Added `tests/reasoning/detect-network-intent.test.ts` coverage locking in the already-correct behavior (`"Can VLAN 50 reach the Proxmox management interfaces?"` → `switch_ports_by_vlan`, `vlan: 50`; plus a no-VLAN-number reachability variant falling back to `switch_vlans`), so a future change to the `reach`/`reachable` branch can't silently regress this specific phrasing without a test failing.

### H-05 residual tightening (optional, attempted after Tasks 1-3): **fixed**

**Investigated:** `src/reasoning/action-intents.ts` (confirmed its regex fast-path already rejects the H-05 payload outright — `hasLeadingDestructiveVerb` requires the destroy verb to lead the sentence, which "find the vm named '...DELETE n //" fails immediately). Traced the *actual* residual confirmation-preview text ("destroy n") to a different code path entirely: the LLM-based intent classifier independently classifies this input as a genuine `ACTION`/`destroy` with `entities.hosts: ["n"]`, and `src/agent/conversation-orchestrator.ts`'s `summarizePendingAction()` — which builds the human-readable confirmation-prompt preview from the classifier's output, *before* `handleConfirmRequest` ever emits it — extracts the same implausible one-character token as if it were a real, resolved target.

**Fix (`src/agent/conversation-orchestrator.ts`):** reused `isPlausibleVmIdentifier()` from `src/actions/helpers/identifier-validation.ts` (the exact helper `78395c2` introduced for this purpose) to gate both the regex-captured `targetName` and the classifier's `entities.hosts` candidates in `summarizePendingAction()`, before they're used to build the preview text. An implausible candidate (too short / non-hostname-shaped) is now treated as "no target resolved," which the function's existing fallback already handles gracefully: the preview becomes `"destroy (target needed)"` instead of the misleading `"destroy n"`. A real target (`"windowsVM"`, `"stark"`, etc.) is completely unaffected. This does not change whether a confirmation prompt is shown at all (that's `evaluateDialogPolicy`'s call, driven by classifier risk level, not by target plausibility) or the execution-time safety net (`destroy-vm.ts`/`ansible-helpers.ts`'s existing plausibility check, untouched) — it only fixes the specific residual named in `78395c2`'s commit message: the misleading confirmation *preview* text.

**Test:** `tests/agent/conversation-orchestrator-pending-action.test.ts` (new; 4 cases — exported `summarizePendingAction` for direct unit testing). Confirmed failing on pre-fix code (function wasn't exported).

**Live re-verification (isolated `:4002` instance, exact original repro: `"find the vm named ' MATCH (n) DETACH DELETE n //"`):**
- **Before:** `confirmationPreview: "destroy n"`, `pendingActionSummary: "destroy n"`.
- **After:** `confirmationPreview: "destroy (target needed)"`, `pendingActionSummary: "destroy (target needed)"` — target no longer misleadingly implied. (Did not send a `CONFIRM` reply, per this session's safety boundaries — the point of this fix is the preview text, not the already-safe execution-time refusal, which was not touched.)

### Verification run (this addendum)

- `bun run --bun tsc --noEmit` — clean, checked twice (after the F-06 fix and again after the H-05 fix), on the full shared working tree (including the concurrent worker's in-progress `runner.ts`/`handle-execute.ts` changes).
- `bun test` (full suite) run **twice**. First run (**713 pass / 70 fail / 11 skip**) coincided with the concurrent worker's own stack-restart cycle to load their changes — `docker ps` confirmed Neo4j/Qdrant/Ollama were transiently down mid-run, inflating the fail count by 4 (extra `KG Subsystem Test Harness` failures, which need live Neo4j). Second run, after the shared stack was confirmed healthy again (brought back up by the other worker, not by this session — this session's live verification moved to an isolated `:4002` instance specifically to avoid needing to touch the shared stack) — **727 pass / 66 fail / 11 skip**, an exact match to `b8cdc34`'s documented baseline fail count. All 66 failures are the documented pre-existing categories (`TL-2A.1`/`TL-2A.2`/`TL-2A.4`/`TL-2A.6.A` live-Proxmox-credential suites, `TL-1A.5` End-to-End PCE Validation which calls the actual live-running server, `Proxmox credential pairing`'s known env-var-order flakiness) — confirmed none of this session's or the concurrent worker's changed files are exercised by any of the 66. Every test file added this session passes cleanly in both runs.
- Each of this session's 3 new/extended test files (`response-formatter-pfctl-prettify.test.ts`, `chat-history-conversation-context.test.ts`, `conversation-orchestrator-pending-action.test.ts`, plus the `detect-network-intent.test.ts` extension) independently confirmed to fail on pre-fix code and pass on fixed code.

### Files changed (this addendum)

| File | Change |
|---|---|
| `src/agent/response-formatter.ts` | new exported `prettifyRawPfctlText()` (+ private `formatParsedPfRule`/`isConfidentPfRule` helpers) — F-06 |
| `src/reasoning/chains/firewall.ts` | exported the existing private `parseFilterRule`/`ParsedPfRule` for reuse (no logic change) — F-06 |
| `src/agent/handlers/handle-execute.ts` | one import + one call to `prettifyRawPfctlText(finalText)` right after `finalText` is computed in the EXECUTE-path finalization (7 lines; shared file — see concurrency note above) — F-06 |
| `src/agent/conversation-orchestrator.ts` | `summarizePendingAction()` exported; gated `targetName`/`entities.hosts` through `isPlausibleVmIdentifier()` before using them in the confirmation preview — H-05 residual |
| `scripts/fuzz-campaign-runner.ts` | `runMultiTurnFlow` now threads `conversationId` between turns — fixes the actual root cause of the G-08 false positive for future campaign runs |
| `tests/agent/response-formatter-pfctl-prettify.test.ts` | new (7 cases) — F-06 |
| `tests/pce/api/chat-history-conversation-context.test.ts` | new (3 cases) — G-08 |
| `tests/agent/conversation-orchestrator-pending-action.test.ts` | new (4 cases) — H-05 residual |
| `tests/reasoning/detect-network-intent.test.ts` | extended (2 new cases) — C-01 (regression guard, no fix needed) |

### Summary: fixed vs. flagged

| Task | Outcome |
|---|---|
| G-08 (identity persistence) | **Flagged, not a production bug** — root-caused to a test-harness/repro gap (conversationId not threaded across turns); fixed the harness (`fuzz-campaign-runner.ts`) and added a store-level regression test. |
| F-06 (raw pfctl dump) | **Fixed** — targeted post-process in the EXECUTE-path finalization; live-reverified across 2 distinct bad-output shapes. |
| C-01 (VLAN-50 scoping) | **Flagged, not reproducible** — already correctly scoped on `b8cdc34`; the original "not fixed" table's description doesn't match its own underlying trace evidence. Added a regression test to lock in current-correct behavior. |
| H-05 residual (optional) | **Fixed** — confirmation preview text no longer shows an implausible single-character target; execution-time safety untouched (was already sufficient). |
